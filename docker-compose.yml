services:
  analyst:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_PATH: /app/data/esg.db
      TOP_K: ${TOP_K:-6}
      RETRIEVAL_MODE: ${RETRIEVAL_MODE:-bm25}
      USE_LLM: ${USE_LLM:-false}
    volumes:
      - ./data:/app/data
